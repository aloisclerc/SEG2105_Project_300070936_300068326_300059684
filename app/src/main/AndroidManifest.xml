<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projectseg2105">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ApplicationActivity"></activity>
        <activity android:name=".SearchActivity" />
        <activity android:name=".EditDocsDialog" />
        <activity android:name=".DocumentsActivity" />
        <activity android:name=".FormActivity" />
        <activity android:name=".Service_Activity" />
        <activity android:name=".AddBranchActivity" />
        <activity android:name=".EmployeeActivity" />
        <activity android:name=".ViewUserActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".BranchesActivity" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>